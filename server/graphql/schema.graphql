schema {
    query: Query
    mutation: Mutation
}

type Query {
    hero(id: ID): Hero
    heroes: [Hero]
    town(id: ID): Town
    towns: [Town]
}

type Mutation {
    addHero(movementPoints: Int!, name: String!, townId: String!): Hero
    addTown(name: String!): Town
}

type Hero {
    id: ID
    movementPoints: Int
    name: String
    town: Town
}

type Town {
    heroes: [Hero]
    id: ID
    name: String
    type: String
}
